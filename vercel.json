{
  "installCommand": "rm -rf node_modules package-lock.json && npm install && npm ls clsx",
  "buildCommand": "npm run build",
  "crons": [
    {
      "path": "/api/check-gmail",
      "schedule": "*/15 * * * *"
    }
  ]
}